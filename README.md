# 序界 NEXUS 博客

一个可直接部署到 GitHub Pages 的纯静态中文博客，无需构建工具。

## 发布

1. 新建 GitHub 仓库，并将本目录中的全部文件上传到仓库根目录。
2. 打开仓库的 **Settings → Pages**。
3. 在 **Build and deployment** 中选择 **Deploy from a branch**。
4. 选择 `main` 分支和 `/(root)` 目录，保存。

稍等片刻后，GitHub 会显示博客网址。编辑 `index.html` 和 `posts` 文件夹即可更新内容。
